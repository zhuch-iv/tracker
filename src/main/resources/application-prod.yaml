quarkus:
  datasource:
    db-kind: postgresql
    username: tracker
    reactive:
      url: postgresql://localhost:5432/tracker
  hibernate-orm:
    log:
      sql: false
      format-sql: false
    database:
      generation: update
